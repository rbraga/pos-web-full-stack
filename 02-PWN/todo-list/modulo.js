// console.log('Estou em -> modulo.js');

const sobrenome = 'Braga';

const calculoMaluco = (numero, valorIncremento) => {
    return (numero * valorIncremento + 2) / 10;
}

module.exports = calculoMaluco;