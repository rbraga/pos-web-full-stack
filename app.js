const calculoMaluco = require('./modulo');
const log = require('./util');

const nome = 'Rafael';

const resultado = calculoMaluco(10, 2);

// console.log('Estou em -> app.js');
// console.log(`${nome} ${sobrenome}`);

console.log(resultado);


log("primeiro mensagem de log");